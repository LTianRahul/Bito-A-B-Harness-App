# Per-machine secrets/state — never bake these into the image, they live in
# the /data volume instead (see docker/entrypoint.sh).
configs/
runs/
judgments/
reports/
prompt_sets/
results.db
results.db-*
prompts.json
indexed-repos.txt
*.bak-*

# Python
.venv/
__pycache__/
*.pyc

# Node
frontend/node_modules/
frontend/tsconfig.node.tsbuildinfo
frontend/tsconfig.tsbuildinfo

# VCS / OS junk
.git/
.DS_Store

# Docs aren't needed inside the image
docs/
